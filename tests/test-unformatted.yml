name: Test unformatted - unformatted Python fails black check
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - id: action
        uses: .
        continue-on-error: true
        with:
          path: tests/fixtures/bad.py

      - name: Assert black check failed on unformatted file
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" != "failure" ]; then
            echo "FAIL: expected black to fail on unformatted code, but outcome was '$OUTCOME'"
            exit 1
          fi
          echo "PASS: black correctly rejected unformatted code"
