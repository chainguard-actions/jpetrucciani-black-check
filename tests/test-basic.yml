name: Test basic usage - well-formatted Python file
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - id: action
        uses: .
        with:
          path: tests/fixtures/good/good.py

      - name: Assert black check passed on well-formatted file
        run: |
          echo "PASS: black check succeeded on well-formatted file (exit 0)"
